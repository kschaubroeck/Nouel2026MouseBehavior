% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostic.R
\name{simulate_residuals}
\alias{simulate_residuals}
\title{Simulate DHARMa residuals for multiple models}
\usage{
simulate_residuals(
  x,
  n = 1000,
  seed = 12345,
  mode = c("unconditional", "conditional", "zero"),
  tmb_terms = "ALL",
  ...
)
}
\arguments{
\item{x}{A named or unnamed list of fitted model objects.}

\item{n}{Integer; number of simulations passed to each single-model call.}

\item{seed}{Integer; random seed for reproducibility.}

\item{mode}{Character; one of "unconditional", "conditional", or "zero".}

\item{tmb_terms}{Character; terms argument passed to \code{glmmTMB::set_simcodes}.}

\item{...}{Additional arguments passed to \code{simulate_residuals0()} /
\code{DHARMa::simulateResiduals()}.}
}
\value{
A list of DHARMa simulated residual objects (one per element of \code{x},
\code{NULL} for any model whose simulation failed).
}
\description{
Apply \code{simulate_residuals0()} over a list of fitted model objects and
return a list of DHARMa simulated residual objects. This is a convenience
wrapper that iterates over \code{x}, passing each element's name (or integer
position when \code{x} is unnamed) as the \code{name} identifier used in diagnostics
and error messages.
}
\details{
Errors thrown for individual models are caught with \code{rlang::try_fetch},
re-emitted as warnings that identify the failing model by name/position,
and replaced with \code{NULL} in the output list so that the remaining models
are still processed.
}
