% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{loocv}
\alias{loocv}
\title{Leave-one-group-out cross-validation}
\usage{
loocv(model, data, group)
}
\arguments{
\item{model}{A fitted model object (e.g. from \code{lme4::lmer()},
\code{lme4::glmer()}, \code{glmmTMB::glmmTMB()}, \code{stats::lm()}, or \code{stats::glm()}).}

\item{data}{A data frame containing all variables required by \code{model}.}

\item{group}{Character string naming the column in \code{data} whose unique
values define the cross-validation folds.}
}
\value{
A list with three elements:
\describe{
\item{\code{data}}{The input data frame augmented with columns \code{.obs}
(observed response), \code{.pred} (out-of-fold expected value), \code{.disp}
(dispersion), \code{.problem} (logical fit-problem flag), \code{.ld}
(log-density), and \code{.prob} (cumulative probability).}
\item{\code{coefs}}{A data frame of per-fold fixed-effect coefficient
estimates with columns \code{group}, \code{term}, \code{estimate}, and \code{probs}
(logical fit-problem flag).}
\item{\code{family}}{Character string naming the response distribution.}
}
}
\description{
Performs leave-one-group-out cross-validation (LOGOCV) for a fitted model.
For each unique value of \code{group}, the model is re-fitted on all remaining
observations and used to predict the held-out group. Fit diagnostics,
predicted values, dispersions, log-densities, and cumulative probabilities
are returned alongside the original data.
}
