% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_analysis}
\alias{plot_analysis}
\title{Plot Analysis Results}
\usage{
plot_analysis(
  .results,
  .covariates,
  ...,
  .with_data = FALSE,
  .without_mean = FALSE,
  .layout = NULL,
  .split = NULL,
  .styles = NULL,
  .extra = NULL,
  .writer = NULL,
  .alpha = if (.with_data) 0.4 else 0.7,
  .seed = 12542
)
}
\arguments{
\item{.results}{Named list of data frames with analysis results. Each data frame
must contain an \code{adjusted} column and typically includes \code{ci_lower} and
\code{ci_upper} for confidence intervals.}

\item{.covariates}{Tidyselect specification (one or more columns) used to form the x-axis.}

\item{...}{Reserved for future arguments; currently unused.}

\item{.with_data}{Logical; if TRUE, raw data points from the original \code{data}
attribute are plotted (default FALSE).}

\item{.without_mean}{Logical; if TRUE, the estimated means and their errorbars are omitted.}

\item{.layout}{Optional Facet object (from \code{facet()}) used for faceting.}

\item{.split}{Optional column name(s) used to split each result data frame into multiple plots.}

\item{.styles}{Optional Styles object from \code{new_styles()} to control scales and appearance.}

\item{.extra}{Optional list of quosures containing additional ggplot2 layers to add.}

\item{.writer}{Optional PlotOutput object from \code{plot_writer()}; when provided,
plots are written to disk and file paths are returned invisibly.}

\item{.alpha}{Numeric alpha transparency for raw data points; default depends on \code{.with_data}.}

\item{.seed}{Integer seed used for reproducible jittering of raw points (default 12542).}
}
\value{
If \code{.writer} is provided, invisibly returns saved file paths; otherwise
returns a named list of \code{ggplot} objects corresponding to \code{.results}.
}
\description{
Visualize model-adjusted results with optional raw data overlay, faceting,
styling, and the ability to split result sets and write output to disk.
}
