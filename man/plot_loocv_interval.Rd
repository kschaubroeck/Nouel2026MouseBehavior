% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{plot_loocv_interval}
\alias{plot_loocv_interval}
\title{Plot leave-one-group-out prediction intervals}
\usage{
plot_loocv_interval(data, by = NULL, alpha = 0.05, show_expected = FALSE)
}
\arguments{
\item{data}{The list returned by \code{loocv()}.}

\item{by}{Optional character vector of column names to use as faceting
variables. When \code{NULL} (default) no faceting is applied.}

\item{alpha}{Significance level used to construct the prediction interval.
Defaults to \code{0.05} (giving a 95\\% interval).}

\item{show_expected}{Logical; if \code{TRUE}, the predicted (expected) value for
each observation is also drawn as a small cross marker. Defaults to
\code{FALSE}.}
}
\value{
A \code{ggplot2} plot object.
}
\description{
Visualises the out-of-fold prediction intervals produced by \code{loocv()}. Each
row of the data is shown as a point (observed value) overlaid on a
horizontal error bar spanning the \verb{(alpha/2, 1-alpha/2)} quantiles of the
fitted distribution. Points are coloured according to whether they fall
inside the interval, outside it, or are associated with a model fit
problem.
}
