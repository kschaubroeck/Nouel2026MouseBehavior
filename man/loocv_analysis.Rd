% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{loocv_analysis}
\alias{loocv_analysis}
\title{Leave-one-group-out cross-validation with a custom analysis function}
\usage{
loocv_analysis(.model, .f, .data, .group, ...)
}
\arguments{
\item{.model}{A fitted model object (e.g. from \code{lme4::lmer()},
\code{lme4::glmer()}, \code{glmmTMB::glmmTMB()}, \code{stats::lm()}, or \code{stats::glm()}).}

\item{.f}{A function (or lambda) accepting \verb{(model, test_data, ...)} and
returning a single data frame of results for the held-out fold. Can be
any object coercible by \code{\link[rlang:as_function]{rlang::as_function()}}.}

\item{.data}{A data frame containing all variables required by \code{.model}.}

\item{.group}{Character string naming the column in \code{.data} whose unique
values define the cross-validation folds.}

\item{...}{Additional arguments passed to \code{.f}.}
}
\value{
A data frame containing the row-bound outputs of \code{.f} across all
}
\description{
Performs leave-one-group-out cross-validation (LOGOCV) for a fitted model,
applying a user-supplied analysis function to each held-out fold. For each
unique value of \code{.group}, the model is re-fitted on all remaining
observations and \code{.f} is called with the refitted model and the held-out
data. Results are row-bound and annotated with the fold identifier and a
fit-problem flag.
}
