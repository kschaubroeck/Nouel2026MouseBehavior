% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output.R
\name{combine_data}
\alias{combine_data}
\title{Combine multiple data files into a single file}
\usage{
combine_data(
  .dir,
  .outfile,
  ...,
  .adjust = c("none", "bonferroni", "BH", "fdr")
)
}
\arguments{
\item{.dir}{Path to a directory containing data files to combine.}

\item{.outfile}{Path to the output file to write. Must have extension
'csv', 'tsv', or 'rds'. The output directory will be created if needed.}

\item{...}{Additional arguments passed on to the appropriate \code{readr}
reader (\code{read_csv}, \code{read_tsv}, \code{read_rds}) when reading input files and
to \code{write_data_file()} when writing the combined output.}
}
\value{
Invisibly returns the combined data frame created by row-binding
the readable input files.
}
\description{
Reads all data files with extensions \code{csv}, \code{tsv}, or \code{rds} from a directory,
row-binds them together, and writes the combined table to the specified
output file. Any files that fail to read are skipped with a warning.
}
